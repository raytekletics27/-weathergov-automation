terraform {
  required_version = ">= 1.0"
  required_providers {
    # Example placeholder:
    # aws = {
    #   source  = "hashicorp/aws"
    #   version = "~> 4.0"
    # }
  }
}

# Add provider and resources when you're ready.
# Example provider (commented out):
# provider "aws" {
#   region = var.region
# }
